[package]
name = "gump-driver"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "gump.driver/1 trait, native/script lifecycle (F06)."

[dependencies]
gump-types = { workspace = true }

[dev-dependencies]
