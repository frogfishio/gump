[package]
name = "gump-telemetry"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Ratatouille adapter (T01), stream capture (T02), local ring/subscriber (T03)."

[dependencies]
gump-types = { workspace = true }
ratatouille = "0.1"
serde = { workspace = true, features = ["derive"] }
serde_json = "1.0"
