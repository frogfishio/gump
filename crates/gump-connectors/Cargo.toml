[package]
name = "gump-connectors"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Object, identity, publication, and output connector contracts (D01–D02+)."

[dependencies]
gump-types = { workspace = true }
blake3 = { workspace = true }

[dev-dependencies]
