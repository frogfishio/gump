[package]
name = "gump-scheduler"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Feasibility, reservations, scoring, and admission (RUNTIME §1–2 / GUMP-N011)."

[lib]
path = "src/lib.rs"

[dependencies]
gump-types = { workspace = true }

[dev-dependencies]
