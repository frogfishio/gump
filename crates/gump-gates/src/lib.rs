//! Workspace quality gates for W01+.
//!
//! Product crates live under `crates/gump-*` per docs/v1/README.md §5.
//! This crate is tooling only and must not become a runtime dependency.

#![forbid(unsafe_code)]
