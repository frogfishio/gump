[package]
name = "gump-gates"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Workspace quality gates (W01 dependency direction and membership)."

[lib]
path = "src/lib.rs"
