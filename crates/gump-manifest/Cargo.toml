[package]
name = "gump-manifest"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Parse, normalize, and validate gump/1 manifests (F01)."

[dependencies]
gump-types = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }
