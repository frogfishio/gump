[package]
name = "gump-protocol"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Protobuf messages, frame limits, and golden vectors (W03)."

[dependencies]
prost = "0.14"
bytes = "1.10"

[build-dependencies]
prost-build = "0.14"
protoc-bin-vendored = "3.1"

[dev-dependencies]
