[package]
name = "gump-protocol"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Protobuf messages, frame limits, goldens, and session negotiation (W03/C01)."

[dependencies]
prost = "0.14"
bytes = "1.10"

[build-dependencies]
prost-build = "0.14"
protoc-bin-vendored = "3.1"

[dev-dependencies]
