schema = "gump/1"

[app]
id = "hello-job"
namespace = "default"

[workload]
lifetime = "finite"
coordination = "independent"
success = "all_exit_zero"

[package]
root = "."
include = ["bin/hello"]

[runtime]
driver = "native"
command = ["./bin/hello"]
stop_timeout = "10seconds"
